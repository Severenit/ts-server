{
  "version": 2,
  "builds": [
    { "src": "src/**/*.ts", "use": "@vercel/node" }
  ],
  "routes": [
    { "src": "/webhook", "dest": "/src/api/webhook.ts" },
    { "src": "/(.*)", "dest": "/src/server.ts" }
  ]
} 